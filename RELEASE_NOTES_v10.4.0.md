# Lattice AI 10.4.0 — Named Ground

릴리스일: 2026-08-02

## 한 줄 요약

**10.3.0이 "측정되지 않은 것"을 적어두고 끝냈다면, 10.4.0은 그 목록을 비웠습니다.**
mypy 백로그 77개 모듈 → 0개, `app_factory._build` 1,318줄 → 26줄, 표면 패리티의
◐(부분 제공) → 0개. 그 과정에서 진짜 결함 네 개가 나왔습니다.

---

## 1. 타입 백로그의 68%는 타입 문제가 아니었습니다

10.3.0은 77개 모듈에 **1,407개**의 mypy 오류가 남아 있다고 정직하게 적었습니다.
그중 두 개의 원인이 954개를 만들고 있었고, 둘 다 **읽기 어려움**의 문제였습니다.

### `__all__`이 계산되고 있었습니다 (약 750개)

```python
# lattice_brain/graph/_kg_common.py
__all__ = [name for name in globals() if not name.startswith("__")]
```

런타임에는 맞습니다. 타입 검사기에는 **완전히 불투명**합니다.
`from ._kg_common import *` 하는 12개 그래프 모듈에서 **단 하나의 이름도 해석되지
않아**, 각 파일이 수십~수백 개의 가짜 `name-defined` 오류를 내며 검사 대상 밖에
남아 있었습니다.

리터럴로 고정하니 750개가 한 번에 사라졌습니다.
`tests/unit/test_kg_common_exports.py`가 이 리터럴이 계산식과 여전히 같은지를
검증하므로, 다시 어긋날 수 없습니다.

### 11개 믹스인이 공유하던 계약이 어디에도 없었습니다 (229개)

`KnowledgeGraphStore`는 믹스인 11개로 조립됩니다. `ingest`가 `write_master`의
`_upsert_node`를 부르고, 그게 다시 `projection`의 `_v2_project_node`를 부르고,
전부가 `_connect`를 씁니다. 그 계약은 **존재했지만 코드 어디에도 적혀 있지
않았고**, 읽는 사람은 11개 파일을 grep해야 알 수 있었습니다.

`lattice_brain/graph/_kg_contract.py`가 그 계약을 씁니다 — 공유 표면 23개.
**타입 전용**입니다: 런타임에는 `_Core`가 `object`라 MRO가 바이트 단위로
동일하고, `tests/unit/test_kg_contract.py`가 그 사실을 못 박습니다.

### 남은 것

나머지 453개는 실제 애노테이션 작업이었습니다. 결과:

```
10.2.0:  13 / 270 모듈
10.3.0: 193 / 270 모듈,  1,407 오류 잔여
10.4.0: 274 / 274 모듈,      0 오류 잔여
```

`docs/MYPY_BACKLOG.md`는 이제 "남은 목록"이 아니라 **어떻게 닫았는지의 기록**입니다.

---

## 2. 조립 루트가 함수 하나가 아닙니다

`app_factory._build`는 **1,318줄짜리 단일 클로저**였습니다. 그게 우연이 아니었던
이유가 있습니다: 여러 섹션이 **더 아래에서** 바인딩되는 이름을 참조합니다.
`save_to_history`는 약 150줄 뒤에 만들어지는 `append_audit_event`를 호출합니다.
한 함수 안에서는 동작합니다 — 클로저는 자유 변수를 **정의 시점이 아니라 호출
시점에** 해석하니까요. 순진한 추출은 정확히 거기서 깨집니다(10.3.0이 겪고
기록했습니다).

`RuntimeContext`가 그 추출을 안전하게 만듭니다. 각 단계가 쓰고 읽는 가변
객체이므로, 2단계에서 만든 클로저가 `ctx.append_audit_event`를 호출할 때도 여전히
호출 시점에 해석됩니다 — **같은 늦은 바인딩, 이제는 상태에 이름이 붙은 채로.**

```
1. platform          MLX/GPU 장치 선택 (하드웨어를 만지는 유일한 단계)
2. config            설정, 보안, 경로, 파일시스템 레이아웃
3. identity          사용자·세션·감사·접근제어·API 키·SSO/VPC
4. brain             임베더·지식그래프·대화·훅·영속화·히스토리
5. domain            모델 라우터·정원·채팅 서비스   ← web이 라우터를 필요로 함
6. web               라이프스팬·FastAPI 앱·모델 런타임·정적/기반 라우터
7. services          검색/컨텍스트·에이전트 런타임·타입드 AppContext
8. foundation_routes AppContext가 생긴 뒤 기반 라우터 마운트
9. platform_features 워크스페이스 플랫폼·자동화·리뷰·커맨드센터
10. interaction      모델/채팅/검색/도구 라우터 + 브레인 tail 라우터
```

```
app_factory.py       1,444 → 251줄   (_build: 1,318 → 26줄)
runtime/build_phases.py       + 1,415줄  (10개 단계)
runtime/runtime_context.py    +   276줄  (선언된 공유 상태)
```

**순서는 주석이 아니라 계약입니다.** `tests/unit/test_runtime_context.py`가
단계 순서, "어떤 단계가 어떤 값을 만드는지", 그리고 **아직 만들어지지 않은 값을
읽으면 그 속성 이름과 함께 실패하는지**를 고정합니다. `RuntimeContext`의 속성은
기본값 없이 선언만 되어 있습니다 — 기본값이 있었다면 순서 위반이 `None`으로
조용히 통과했을 것입니다.

---

## 3. 그 과정에서 나온 진짜 결함 네 개

| 결함 | 위치 | 왜 중요한가 |
| --- | --- | --- |
| `return` 뒤 4줄 중복 | `core/workspace_os.py` | 병합 이후 죽어 있던 코드. `remove_member`가 일을 두 번 하는 것처럼 보였습니다 |
| **`main`이 내보내진 적이 없음** | `app_factory.py` | `python -m latticeai.server_app`이 `AttributeError`로 죽었습니다 — 문서화된 실행 경로가 동작하지 않았습니다 |
| `str`가 `dict`를 가림 | `models/router.py` | 커스텀 클라우드 모델 분기가 **잘못된 변수로 id를 만들고** 있었습니다 |
| 미설정 seam을 무방비 호출 | `lattice_brain/context.py` | 설정되지 않은 검색 포트가 실패 격리 안에서 `TypeError`로 죽으며 자기 이름을 말하지 않았습니다 |

`main`은 특히 조용한 종류입니다. `_build` 안의 지역 함수였고 export 허용목록에
없었으므로, `get_shared_runtime().main()`은 언제나 실패했습니다. 테스트는 전부
`create_app()`을 쓰기 때문에 아무것도 잡지 못했습니다. 이제 `main()`은 공유
런타임을 직접 서빙하며 export가 아예 필요 없습니다.

---

## 4. 표면 패리티: 마지막 ◐ 세 개

세 갭 모두 **계약이 아니라 렌더링의 문제**였습니다. 사이드카는 이미 데이터를
내고 있었고, 에디터와 봇이 그것을 버리고 있었습니다.

| 표면 | 이전 | 지금 |
| --- | --- | --- |
| VS Code 수집 | ◐ 현재 파일만 | ✅ `ltcai.captureFolder` — 웹과 **같은** `/api/ingestion/folder`, 같은 승인 절차 |
| VS Code 산출물 | ◐ 평평한 파일 목록 | ✅ `ltcai.showArtifacts` — `artifacts[]` 카드, 보정/검증 플래그 포함 |
| VS Code 모델 | ◐ 카탈로그만 | ✅ `/setup/scan` 하드웨어 추천 + **왜 이 기기에 이 모델인지** 근거 |
| Telegram 산출물 | ◐ 파일 전송만 | ✅ 전송 직전 `artifacts[]` 카드 요약 |

가장 중요한 차이는 **결정적으로 보정된(repaired) 스캐폴드와 깨끗한 모델 출력이
이제 구분된다**는 것입니다. 평평한 경로 목록에서는 둘이 똑같아 보였습니다.
확장과 봇은 서버의 `valid=false` 판정을 성공으로 승격하지 않으며, 구형
사이드카 응답에는 "세부 정보 없음"이라고 정직하게 말합니다.

`docs/SURFACE_PARITY.md`의 표에 ◐ 는 이제 **없습니다**. 남은 "—"는 전부 이유가
적힌 설계 경계입니다.

---

## 5. 커버리지: 정직한 부분 진전

| | 10.3.0 | 10.4.0 |
| --- | ---: | ---: |
| Python 테스트 (전체) | 1,896 | **1,956** |
| Python 커버리지 | 71.6% | **71.8%** |
| 프론트 테스트 | 208 | **337** |
| 프론트 커버리지 | 28.5% | **32.3%** |

프론트 테스트를 붙인 곳은 전부 **로직이 있는데 테스트가 없던** 모듈입니다:
라우팅(별칭 하나가 어긋나면 조용히 엉뚱한 페이지로 갑니다), 앱 스토어(저장소가
꺼진 브라우저에서의 동작), API 오류 번역("로컬 서비스에 연결할 수 없습니다" vs
날것의 `TypeError`), 지식그래프 탐색기(순수 로직인데 4% 커버리지였습니다).

**목표에 도달했다고 말하지 않겠습니다.** 프론트 70%까지는 아직 약 2,200
statement가 남아 있고, 대부분은 큰 React 페이지 컴포넌트입니다.

---

## 정직한 자기 평가

| 영역 | 10.2.0 | 10.3.0 | 10.4.0 | 남은 것 |
| --- | ---: | ---: | ---: | --- |
| 아키텍처·모듈성 | 16 | 16 | **19** | 라우터 등록 단계의 kwarg 폭 |
| 테스트·검증 | 16 | 17 | **18** | 프론트 32→70%, Python 72→85% |
| 보안·프라이버시 | 18 | 19 | **19** | 서버 i18n, 남은 S-룰 면제 |
| 코드 품질·툴링 | 17 | 18 | **19** | `UP` 룰 그룹 미적용 |
| 릴리스·문서·정직성 | 20 | 20 | **20** | — |
| | **87** | **88** | **95** | |

아키텍처가 16 → 19인 이유는 `_build` 분해가 실제로 끝났기 때문입니다. 20이
아닌 이유는 `router_registration`의 등록 함수들이 여전히 20~50개 kwarg를 받기
때문입니다 — 다음 후보입니다.

코드 품질이 20이 아닌 이유는 mypy는 끝났지만 ruff `UP` 룰 그룹이 아직 켜져
있지 않기 때문입니다.

테스트가 17 → 18에 그친 이유는 정직하게, 커버리지가 아직 목표의 절반이기
때문입니다. 새 테스트 195개(Python 60 · 프론트 129 · VS Code 6)는 전부 이전에
검증된 적 없던 경계에 붙었지만, 숫자 자체는 아직 낮습니다.

## 검증

| 게이트 | 결과 |
| --- | --- |
| pytest (unit) | 1,953 passed |
| pytest (전체 + coverage) | 1,956 passed · 71.77% (하한 70% 강제) |
| vitest | 337 passed · 32.31% |
| mypy | **274 / 274 modules, 0 errors** |
| ruff | All checks passed |
| VS Code 확장 계약 | 19 passed |
| brain/agent eval · product readiness | pass |
