# Lattice AI 9.9.9 — Lean Shell

릴리스일: 2026-07-27

## 한 줄 요약

첫 화면이 받아야 하는 JavaScript를 **150.0 KiB → 99.3 KiB (gzip, -34%)** 로
줄였습니다. 9.9.8에서 임시로 올렸던 번들 예산은 원래의 150 KiB로 되돌렸고,
이번에는 그 아래에 실제 여유가 생겼습니다.

## 무엇이 문제였나

9.9.8에서 자율성 다이얼 설정 패널을 추가할 때 번들 예산(150 KiB)이 터졌습니다.
당시 진단은 이랬습니다 — 페이지 컴포넌트는 이미 lazy인데, `frontend/src/i18n/*`가
**하나의 동기 테이블**이라 `t()`가 읽는 모든 화면의 문구가 구조적으로 첫 페인트
경로에 들어간다는 것. 즉 UI 표면을 추가할 때마다 초기 번들이 커지는 구조였고,
9.9.7 시점에 이미 여유가 0.7 KiB뿐이었습니다.

9.9.8은 예산을 152 KiB로 올리고 사유를 기록하는 선에서 멈췄습니다. 9.9.9는
원인을 고칩니다.

## 어떻게 고쳤나

네임스페이스가 **import 시점에 스스로 등록**하도록 바꿨습니다.

- `frontend/src/i18n/registry.ts` — 공유 가변 테이블 + `registerCopy()`
- `shell` 네임스페이스만 즉시 등록(앱 프레임, 언어 전환, 공통 `ui.*`)
- `brain` / `workspace` / `onboarding`은 **각 lazy 라우트의 청크 안에서** 등록

각 lazy 라우트가 자기가 읽는 네임스페이스만 `import` 합니다. 예를 들어
`pages/System.tsx`는 `@/i18n/workspace`만, `pages/Brain.tsx`는 `brain`과
`workspace`를 가져옵니다.

곁가지로 **Admin 콘솔도 lazy**로 내렸습니다. 드물게 쓰는 별도 표면인데 workspace
문구 전체를 초기 번들로 끌어오고 있었습니다.

## 조용히 깨질 수 있는 지점과 방어

이 분할의 실패 모드는 **조용합니다**. 어떤 컴포넌트가 자기 청크에 없는
네임스페이스의 키를 읽으면 `t()`가 키 문자열을 그대로 반환합니다. 한국어 대신
`system.permission.title`이 화면에 찍히는데, 에러도 없고 그 컴포넌트를 직접
렌더링하는 테스트가 없으면 통과합니다.

그래서 `scripts/check_i18n_namespace_coverage.mjs`를 추가했습니다. 실제 모듈
그래프를 걸어서, 진입점(eager 루트 + 모든 `React.lazy` 경계)마다

1. 정적 import 폐포를 계산하고
2. 그 안에서 쓰이는 모든 `t(lang, "key")` 키를 모으고
3. 그 청크가 import한 네임스페이스로 전부 커버되는지 확인

합니다. 안 되면 빌드가 실패하고, 어느 파일에 어떤 `import "@/i18n/<ns>";`를
넣어야 하는지 알려줍니다. `npm run lint`에 포함됩니다.

타입 전용 import(`import { type X }`)는 런타임 엣지가 아니므로 커버리지로
치지 않습니다.

## 결과

| 항목 | 9.9.8 | 9.9.9 |
|------|-------|-------|
| 초기 JS (gzip) | 150.0 KiB | **99.3 KiB** |
| 초기 JS (raw) | 505.1 KiB | **317.3 KiB** |
| 첫 페인트에서 제외된 lazy 청크 | 25 | **32** |
| 번들 예산 | 152 KiB (임시 상향) | **150 KiB (원복)** |

검증: 실제 브라우저 시각 스모크 18개 통과 — 라우트 전환 시 문구가 정상
렌더링됨을 확인했습니다.

## 그 외

- `frontend/src/test/setup.ts`가 모든 네임스페이스를 등록합니다. 단위 테스트는
  라우트 없이 컴포넌트를 직접 렌더링하므로 필요하며, 프로덕션 커버리지는 위
  스크립트가 별도로 증명합니다(테스트 설정에 의존하지 않습니다).
- `brain.title`(제품 워드마크)은 라우트 해석 전에 렌더링되므로 `shell`
  네임스페이스로 옮겼습니다.

## 호환성

동작 변화 없음. 문구, 라우팅, API 모두 9.9.8과 동일합니다. 바뀐 것은 브라우저가
첫 페인트에 무엇을 내려받는가뿐입니다.
