# Lattice AI 10.1.1 — Reachable Boundary

릴리스일: 2026-07-28

## 한 줄 요약

10.1.0은 하이브리드 경로를 **전부 만들어 놓고 화면에 붙이지 않았습니다.**
이번 릴리스가 그 컨트롤입니다.

## 무엇이 빠져 있었나

10.1.0에는 계약·API·`/chat` 분기·정책 저장소·토큰 가드가 모두 들어 있었지만,
사용자가 **닿을 방법이 없었습니다.** 다이얼은 `/api/network-boundary`를 직접
호출하는 사람에게만 존재했고, 그래서 모든 사용자가 선택지를 받아본 적도 없이
`local_only` 기본값에 머물렀습니다.

기능이 위험했던 건 아닙니다 — **빠져서 안전한** 쪽이었습니다. 다만 "다이얼이
있다"는 말은 켤 수 있을 때만 참입니다.

## 이번에 붙인 것

**환경설정 → 내 지식이 나가는 범위**, 에이전트 자율성 다이얼 바로 옆입니다.
"이게 알아서 어디까지 해도 되나"라는 같은 종류의 결정이 이미 사는 자리입니다.

자율성 다이얼과 같은 규칙을 따릅니다:

- 모드 목록을 **서버 카탈로그에서 받아** 그립니다. 클라이언트에 하드코딩된
  모드 목록은 없습니다.
- 서버가 요구하는 **확인 체크를 하기 전까지 클라우드 전환 요청을 보내지
  않습니다.** 서버가 거절할 요청은 클라이언트도 거절합니다.

## 자율성 다이얼에는 없는 것 하나 — 미리 보기

질문을 입력하면 `/api/network-boundary/preview`가 **실제로 나갈 기억의
제목**과 토큰 추정치, 그리고 토큰 가드가 이 턴을 거절할지를 돌려줍니다.

```
무엇이 나가는지 미리 보기
┌──────────────────────────┐  ┌────────────┐
│ 릴리스 어떻게 하지        │  │ 미리 보기   │
└──────────────────────────┘  └────────────┘

지금은 로컬만으로 설정돼 있어 아무것도 나가지 않습니다.
아래는 클라우드를 허용했을 때 나갈 내용입니다.

기억 2개 · 약 412토큰
 · 릴리스 절차 정리
 · 배포 전 확인 목록
```

**로컬만 상태에서도 동작하고, 그렇다고 말합니다.** 결정한 뒤가 아니라 결정하기
전에 볼 수 있어야 의미가 있습니다. "관련된 최소 노드만 나갑니다"는 약속이고,
어떤 노드인지 나열하는 것은 증거입니다.

## 아무것도 못 하는 스위치는 보여주지 않습니다

기록 방식 스위치(`auto_commit`, `allow_multimodal`)는 **클라우드가 허용된
뒤에만** 나타납니다. 작동하지 않는 스위치는 작동한 것처럼 읽힙니다.

## 지운 것

`static/app/network-boundary-panel.js` — 10.1.0이 이 기능의 "UI"로 함께 낸,
어디에도 mount되지 않은 독립 모듈입니다.

이 파일은 `static/app/` 아래 있었는데 그곳은 **Vite의 빌드 출력 디렉터리**라,
`npm run build:assets`가 통째로 지웁니다. 손으로 쓴 파일이 빌드에서 살아남을
수 없는 위치였고, 실제로 직전 릴리스의 에셋 재빌드 때 이미 삭제된 상태였습니다.
React 패널이 그 자리를 대신합니다.

## 바뀌지 않은 것

- 기본값은 여전히 `local_only`
- 민감·비공개·공유금지·로컬전용 기억은 **양쪽 모드 모두에서** 제외
- 클라우드가 찾아낸 내용은 여전히 검토 센터에 **제안으로** 들어가고, 승인해야
  기억이 됨
- 그래프 자체는 나가지 않음 — 한 턴 분량의 최소 노드 조각만

## 검증

| 게이트 | 결과 |
| --- | --- |
| `NetworkBoundaryPanel.test.tsx` | 12/12 |
| Playwright (실제 Chromium) | 패널 도달·확인 게이트·미리 보기 2개 스펙 |
| pytest | 1765 passed / 11 skipped |
| 프론트 단위 전체 | 166/166 |
| 시각 테스트 전체 | 20/20 |
| lint · typecheck · docs · i18n | 통과 |
| 번들 예산 | 150 KiB gzip 이내 |

mock 서버에 `/api/network-boundary*` 라우트를 추가해, 릴리스 스크린샷이 이
패널을 "사용할 수 없음"이 아니라 **동작하는 상태로** 담습니다.

## 업그레이드

할 일이 없습니다. 동작 변경 없음, API 변경 없음. 이전에 API로 `cloud_allowed`를
켜 두었다면 그 설정 그대로 패널에 나타납니다.

## 알려진 한계

- 백엔드가 생성하는 일부 안내 문장은 서버 쪽에서 영어로 만들어져 그대로
  표시됩니다. 서버 i18n은 아직 범위 밖입니다.
- 하이브리드 클라우드 경로는 `LATTICEAI_CLOUD_API_KEY` 없이는 동작하지
  않습니다. 키가 없으면 다이얼을 켜도 로컬 경로로 답합니다.
