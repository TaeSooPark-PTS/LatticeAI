# Lattice AI 10.6.2 — Ask First

릴리스일: 2026-08-03

## 한 줄 요약

**10.6.1은 다섯 화면을 다시 짰지만, Brain 대화 홈만은 절반만 다시 짰습니다.**
순서는 바뀌었는데 모양이 그대로였습니다. 10.6.2는 이 화면 하나를 **두 장의
카드로 쪼갭니다** — 지금 할 일 한 장, 대신 고를 수 있는 것 한 장.

문구는 한 글자도 이번 작업의 목표가 아니었습니다. 아래는 실제로 옮기고, 묶고,
줄인 것들입니다. 기능은 하나도 지우지 않았습니다.

---

## 1. 왜 다시 손대야 했나

10.6.1 이후의 Brain 홈은 카드 한 장 안에 다섯 덩어리가 세로로 쌓여 있었습니다.

```
┌─ 스테이션 ────────────────────────┐
│  🧠  (5.4rem, 가운데)             │
│  말하고, 넣으면, Brain이 연결합니다.│   ← 화면 위 1/3을 인사가 차지
│  기억 8개, 주제 5개 …             │
│  ┌ 입력창 ─────────────────────┐  │
│  └────────────────────────────┘  │
│  이렇게 시작해 보세요 [카드][카드]  │   ← 입력창과 그 도구 사이에 끼어 있음
│  파일 폴더 노트 웹 | 자율성 다이얼  │
└──────────────────────────────────┘
```

두 가지가 동시에 잘못돼 있었습니다.

1. **직접 묻는 것과 골라서 묻는 것이 같은 테두리를 쓰고 있었습니다.** 추천
   질문은 "먼저 할 일"이 아니라 "생각이 안 나면 골라도 된다"는 제안인데,
   위계가 같으니 처음 온 사람에게는 둘 다 똑같이 해야 할 일로 보입니다.
2. **입력창이 자기 도구와 갈라져 있었습니다.** 자료 추가·자율성 줄은 입력창에
   딸린 컨트롤인데, 그 사이에 상관없는 블록이 하나 서 있었습니다.

## 2. 무엇이 바뀌었나 — 표면 셋

```
┌─ 스테이션 (.brain-home-station) ──┐
│ 🧠 말하고, 넣으면, Brain이 …       │   ← 가로 배너. 3.2rem, 제목 옆
│ ┌ 입력창 ──────────────────────┐  │
│ └─────────────────────────────┘  │
│ 파일 폴더 노트 웹 | 자율성 다이얼   │   ← 입력창 바로 아래 (카드 바닥)
└──────────────────────────────────┘
┌─ 덱 (.brain-secondary-deck) ──────┐
│ ✨ 이렇게 시작해 보세요             │   ← 별도 카드로 분리
│ [초점 정리]        [근거 확인]      │
└──────────────────────────────────┘
   지난 대화 2 · Brain이 정리한 내용     ← 조용한 줄 (그대로)
```

- **인사말은 헤드라인에서 배너가 되었습니다.** `flex-direction: column` →
  `row`. 그림 5.4rem → 3.2rem, 제목·기억 수는 그 옆(`.brain-hero-header-text`).
  옅은 배경 그라디언트와 아래 실선으로 카드 머리처럼 닫습니다. 640px 아래에서는
  다시 세로로 쌓이고 가운데 정렬로 돌아갑니다(한 줄에 안 들어갑니다).
- **스테이션은 첫 동작만 담습니다.** 인사 배너 → 입력창 → 도구 줄. 그 사이에
  아무것도 없습니다.
- **추천 질문은 자기 카드로 나갔습니다.** `.brain-secondary-deck` 은 스테이션의
  형제이고, 스테이지(`.brain-centered-home`)의 직계 자식 셋 중 두 번째입니다.
- 스테이지 폭 `44rem → 50rem`, 간격 `0.85rem → 1.1rem`. 카드가 둘이 되면서
  숨 쉴 자리가 필요했습니다.

## 3. 접근성 — 이름은 역할이 있는 요소에 붙습니다

추천 질문 묶음은 `aria-label` 을 평범한 `<div>` 에 달고 있었습니다. **역할이
없는 요소에는 이름을 붙일 수 없어서**, 브라우저가 그 라벨을 버립니다. 스크린
리더에는 이름 없는 덩어리로 도착하고 있었습니다.

- 라벨은 이제 덱의 `<section>` 에 있습니다. 이름이 붙은 `<section>` 은
  `region` 으로 노출됩니다.
- 안쪽 `.brain-home-prompt-strip` div에는 라벨이 **없습니다**. 남겨 두면 다음
  사람이 "div도 라벨을 달 수 있다"고 읽고 같은 실수를 반복합니다.
- 단위 테스트가 `getByRole("region", { name })` 로 이 둘을 함께 잡습니다.

## 4. 빈 상태 — 첫날의 Brain

추천할 질문이 아직 없으면 카드 격자 대신 시작 질문 **알약 줄**이 렌더됩니다.
이 갈래는 지금까지 아무도 보지 못했습니다: mock 서버는 항상 질문 두 개를
돌려주므로 릴리스 스크린샷에도, 기존 테스트에도 카드 격자만 나왔습니다.

- `.brain-prompt-pills-row > button.brain-prompt-pill` 로 이 덱 안에서만
  스코프해 크기를 정합니다. `.brain-prompt-pill` 은 `styles.css` ·
  `conversation.css` 와 공유하는 클래스라, 맨클래스 규칙을 쓰면 알약을 쓰는
  **모든 화면**이 같이 바뀝니다.
- `min-height: 0` 을 명시합니다. `conversation.css` 가 주는 2.65rem 바닥값을
  물려받으면 이 줄의 패딩이 아무 일도 하지 않고, 대신 서 있어야 할 카드보다
  절반쯤 더 높아집니다.
- 알약을 누르면 **보내지 않고 입력창을 채웁니다.** 고르지 않은 질문을 대신
  물어보는 화면이어서는 안 됩니다.
- 테스트가 `**/api/memory/brain-brief*` 응답의 `suggested_questions` 를 비워
  이 갈래를 강제로 렌더합니다.

## 5. 옮기면서 드러난 결함 셋

전부 CSS 우선순위 문제이고, JSX만 읽어서는 하나도 보이지 않습니다.

**후광이 몸통보다 컸습니다.** `LivingBrain` 은 아우라의 `box-shadow` 를 Brain의
깊이에서 계산해 **인라인으로** 씁니다. 인라인 선언은 어떤 시트의 어떤 선택자도
이기지 못하므로, 처음 쓴 `.brain-hero-organism .brain-aura { box-shadow: … }`
규칙은 읽기에는 맞고 실제로는 아무 일도 하지 않았습니다. 58px 그림 주위에
220–320px 짜리 조직을 위해 그린 60px 흐림이 그대로 남았고, 배너의 클립에 잘려
직선 두 개짜리 얼룩으로 찍혔습니다. 이제 값이 `var(--aura-blur, 60px)` 이고
배너 래퍼가 `--aura-blur: 14px` 를 줍니다 — **커스텀 속성은 인라인 값 안으로
상속되기 때문에** 이것만 작동합니다. 폴백 덕분에 다른 호스트는 픽셀 단위로
동일합니다.

**카드를 클립하면 팝오버가 죽습니다.** 자료 추가 팝오버는 스테이션이 아니라
**툴바**를 기준으로 아래로 열립니다. 스테이션에 `overflow: hidden` 을 주면 그
패널이 안 보일 뿐 아니라 카드가 스크롤 상자가 되어, 노트 입력칸에 포커스가
가는 순간 인사말과 입력창 절반이 카드 밖으로 스크롤돼 나갑니다. 스테이션은
`overflow: visible` 로 두고, 배너가 자기 위쪽 모서리만 `clip` 합니다(`hidden`이
아니라 `clip` 인 이유도 같습니다 — `clip` 은 스크롤 상자를 만들지 않습니다).

**감속 모드 취소가 옛 선택자를 가리키고 있었습니다.** `affordance.css` 는 가장
늦게 로드되며 카드형 컨트롤의 hover 리프트와 `prefers-reduced-motion` 취소를
함께 소유합니다. 그 안의 `.brain-home-prompt-strip > button` 은 격자가 생기면서
아무것도 매치하지 않게 되었습니다. hover·감속 선택자는 `.brain-prompt-grid
button` 으로 다시 겨눴고, `white-space` 옵트인 목록에는 **일부러 다시 넣지
않았습니다** — `home-simple.css` 가 900px 아래에서 카드를 `nowrap` 칩으로
뒤집는데, 여기 이름을 올리면 그 미디어 규칙과 특이도가 같아지고 나중에 로드되는
이 시트가 이겨서, 접을 자리가 없는 화면에서 칩이 줄바꿈합니다.

## 6. 검증

**단위 (`frontend/src/features/brain/BrainHome.test.tsx`)**

- 스테이션 안 순서: 인사 → 입력창 → 툴바, 그 다음이 추천 질문.
- 스테이션 안에 `.brain-home-prompt-strip` 도 덱도 **없음**.
- 스테이지의 직계 자식으로 스테이션 → 덱 → 조용한 줄, 이 순서.
- 덱이 `<section>` 이고 이름 있는 `region` 이며, 안쪽 div에는 라벨이 없음.
- 추천이 없을 때 알약 갈래가 렌더됨.

**브라우저 (`tests/visual/v3.spec.js`)** — jsdom이 볼 수 없는 것들:

- 팝오버가 뷰포트 안에 온전히 열리고, 스테이션의 `scrollTop` 이 0을 유지하며,
  인사말이 1px도 움직이지 않음.
- 격자가 덱 안폭의 98% 이상을 채움(`graph-home.css` 의 가운데 정렬이 이기면
  넓은 카드 한가운데 좁은 기둥이 됩니다).
- 900 · 760 · 640 · 420px에서 카드가 칩이 되고, **각 폭에서 사라지지 않음**
  (`responsive.css` 에 760px 아래로 이 클래스를 `display: none` 하는 옛 규칙이
  남아 있습니다).
- 감속 모드에서 `transform` 과 `transition` 이 모두 사라지고, 그래도 테두리
  색으로는 포인터에 답함.
- 후광의 흐림이 그림 지름의 1/3보다 작고, 빛나는 상자가 배너 안에 들어 있음
  (선언이 아니라 **기하학**으로 단언 — 다음에 어떻게 줄이든 유지됩니다).

## 7. 캡처

`npm run release:evidence` 로 12장 전부 다시 찍었습니다. 이전 버전과 픽셀
대조한 결과:

| 캡처 | 변화 |
| --- | --- |
| `04-brain-chat-home.png` | **8.85%** — 이번 작업의 대상 |
| `05-memory-graph.png` | 3.88% — 그래프 레이아웃은 실행마다 다릅니다 |
| `12-review-center.png` | 0.09% — 버전 문자열 |
| 01 · 02 · 03 | 0.00–0.02% — 버전 문자열 |
| 06 · 07 · 08 · 09 · 10 · 11 | 0.00% — 변화 없음 |

의도한 대로 **한 화면만** 바뀌었습니다. `tests/visual/mock_server.cjs` 는
이번에 고칠 것이 없었습니다 — 새 엔드포인트를 쓰지 않고, 12개 화면이 모두 실제
UI로 렌더되는 것을 캡처된 PNG를 열어 확인했습니다.

## 8. 호환성

- 백엔드 동작 변경 없음. `frontend/openapi.json` 은 10.6.1 대비 `info.version`
  만 다릅니다(재생성해 커밋).
- 라우트·딥링크 변경 없음.
- 삭제된 기능 없음. 자료 추가 4종, 자율성 다이얼, 추천 질문, 지난 대화,
  Brain이 정리한 내용 전부 그 자리에 있습니다.

## 9. 남은 것

- 추천 질문 카드는 두 장만 보입니다. mock이 두 개를 주기 때문이고, 격자는
  `auto-fit` 이라 개수에 맞춰 늘어납니다. 실제 Brain이 세 개를 제안하면 세
  장입니다.
- 승인함 카드의 원본 필드 이름(`Action`, `Action Label`, `User Email`)은 10.6.1
  이후 그대로입니다. `FEATURE_STATUS.md` 의 알려진 한계에 남아 있습니다.
