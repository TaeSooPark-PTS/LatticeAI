# V9.9.6 Work Status — 2026-07-27 전체 리뷰 대응

> **작성일:** 2026-07-27 · **결과:** 9.9.6 — Same Brain Everywhere
> 대상: 2026-07-27 코드/UX/하네스/루프 전체 리뷰의 P0·P1·P2 + 루프 항목 + 제품 제안.

## 리뷰 항목 → 구현

### P0 (즉시 체감)

| # | 항목 | 상태 | 구현 |
| --- | --- | --- | --- |
| 1 | 표면 패리티 — VS Code 회상 배지 | ✅ | `vscode-extension/surface.ts::groundingBadge`, `ltcai.askBrain` / `askCurrentFile` → 웹과 동일한 `/chat` `grounding` |
| 1 | 표면 패리티 — VS Code Review Center | ✅ | `ltcai.reviewCenter` → `/api/proposals` list/approve/reject, 409 충돌 정직 보고 |
| 1 | 표면 패리티 — VS Code 스텝 요약 | ✅ | `ltcai.runAgent` → `surface.ts::runReport` (출력 채널) |
| 1 | Telegram/Browser 경계 명시 | ✅ | Telegram `format_run_explanation` + `docs/SURFACE_PARITY.md`에 "—(의도)" vs "✖(갭)" 분리 |
| 2 | 근거 → 행동 원클릭 | ✅ | `POST /api/evidence/actions`, `services/evidence_actions.py`, `AnswerProof.tsx::EvidenceActionRow` |
| 3 | 약모델 실패의 사용자 언어 설명 | ✅ | `core/run_explain.py` → `/agent` `explanation`; 웹 `RunExplanationNote`, VS Code, Telegram |

### P1 (품질 본체)

| # | 항목 | 상태 | 구현 |
| --- | --- | --- | --- |
| 4 | 타입별 청킹 + 인용 품질 | ✅ | `prose` 전략 (문장/문단 경계, 한국어 우선), `citation_locator()`, 청크 metadata → vector 매치 → recall `locator` |
| 5 | Chat/Docgen Context Assembler 단일화 | ✅ | `context_builder`가 `approx_tokens` 예산 + `context_quality_signal` + assembler와 동일한 `trace` 사용 |
| 6 | 개념/관계 노이즈 감소 + 승격 검토 | ✅ | `infer_edge_relation()` (verb / cooccurrence + 가중치), 열거형 문장 차단, `plan_relation_noise_reduction()` |
| 7 | 긴 멀티스텝 프로젝트 상태 | ✅ | `core/project_sessions.py` + `/api/projects` + `/agent`의 `project_id` |

### 루프 엔지니어링

| # | 항목 | 상태 | 구현 |
| --- | --- | --- | --- |
| L1 | 재검색 루프 | ✅ | `core/artifact_ledger.py` + ContextAssembler `Files created in this conversation` 섹션 |
| L2 | Critic 의미 검증 | ✅ | `requirement_coverage()` — 선언된 매니페스트 파일 누락 시 PASS라도 `NEEDS_REVIEW` |
| L3 | 실패 학습 루프 | ✅ | `explain_run()`의 `next_step` → 프로젝트 세션 `last_verification.next_step` → 다음 계획 컨텍스트 |
| L4 | 멀티턴 프로젝트 루프 | ✅ | P1 #7과 동일 |

### P2 (구조·확장)

| # | 항목 | 상태 | 구현 |
| --- | --- | --- | --- |
| 8 | 대형 모듈 추가 분해 | ✅ | `lattice_brain/ingestion_jobs.py`, `latticeai/core/workspace_review_items.py` (동작 보존 이동) |
| 9 | 멀티에이전트 시나리오 densify | ✅ | `test_workflow_scenarios.py` +6 (재시도 소진, 복구, 2중 게이트, 스테일 커서, 역할 관측성, 런 간 격리) |
| 10 | Funnel → 의사결정 | ✅ | `funnel_alerts()` → `GET /api/admin/funnel-metrics`의 `alerts` |
| 11 | 임베딩 교체 후 재인덱싱 UX | ✅ | `StaleEmbedderNotice` — 문제 명명 + 원클릭 재인덱싱 + 정직한 실패 표시 |

### 제품 제안

| 제안 | 상태 | 비고 |
| --- | --- | --- |
| 프로젝트 세션 (Persistent Project Loop) | ✅ | `/api/projects` |
| 근거 기반 액션 카드 | ✅ | `/api/evidence/actions` |
| 개인 지식 가든 뷰 강화 | ◐ | 기존 Living Brain/Brain Care 유지 — 이번 릴리스 범위 밖 |
| 로컬 약모델 전용 에이전트 프로파일 | ◐ | `model_strain` + `next_step`로 진단·안내는 제공, 전용 프로파일은 미구현 |
| 폴더/프로젝트 기억 상태 대시보드 | ◐ | 프로젝트 세션이 파일/할일/검증을 보관; 폴더 단위 대시보드는 미구현 |
| 공유 Brain (opt-in) | — | 기존 enterprise/invitation 경로 유지 |
| 킬러 스킬 패키지 | — | 미착수 |
| 음성/모바일 캡처 루프 | — | 미착수 |

## 남은 갭 (정직하게)

- **VS Code**: 근거→행동 원클릭 없음, 라이브 스텝 SSE 없음(실행 후 요약만).
- **Telegram**: 회상 grounding 배지 없음, Review Center 없음.
- **Browser 확장**: 캡처 전용 — 설계상 의도이며 갭이 아님.
- **Artifact ledger**: 프로세스 로컬·bounded. 재시작 후에는 색인이 따라잡아 일반 검색이 대신 답한다.
- **요구사항 커버리지**: 선언된 파일만 완료를 막는다. 산문형 기능 요구는 critic 판단(권고)으로 남는다.
- 지식 가든 뷰 강화 / 약모델 전용 프로파일 / 폴더 기억 대시보드 / 스킬 패키지 / 음성 캡처는 다음 릴리스 후보.

## 검증

- unit 1670+ (신규: `test_run_explain`, `test_evidence_actions`,
  `test_context_contract_unification`, `test_relation_evidence`,
  `test_project_sessions`, `test_agent_loop_closures`, prose 청킹/로케이터,
  funnel alerts, 분해 계약)
- frontend vitest (evidence actions, run explanation, stale-embedder)
- `node --test tests/vscode-extension.test.cjs` (`npm run lint`에 편입)
- `agent_eval` 23/23, brain quality eval, product readiness gate
- lint / ruff / typecheck / bundle budget / OpenAPI drift / i18n / docs gates
