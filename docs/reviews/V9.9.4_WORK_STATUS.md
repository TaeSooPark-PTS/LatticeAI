# V9.9.4 Work Status — 2026-07-25 리뷰 대응

> **작성일:** 2026-07-26 · **결과:** 9.9.4 — Durable Loops 로 출하 완료
> 대상 리뷰: [CODE_REVIEW_2026-07-25_UX_HARNESS_LOOP_KG_RAG.md](CODE_REVIEW_2026-07-25_UX_HARNESS_LOOP_KG_RAG.md)
> 이 문서는 리뷰의 Wave 0–4 로드맵 항목 → 구현 위치 매핑이다. 역사적 증거로 유지한다.

## Wave 0 — 계약 고정

| 항목 | 상태 | 구현 |
| --- | --- | --- |
| 0.1 승인/run durable store | ✅ | `latticeai/core/run_store.py` (파일당 1런, 토큰 SHA-256, wall-clock 만료, 24h 만료 보존→replan 힌트), `chat_agent_http` 배선, `GET /agent/approvals`; `tests/unit/test_agent_approval_flow.py` 재시작 시나리오 5종 |
| 0.2 RetrievalPolicy 단일화 | ✅ | `lattice_brain/graph/retrieval_policy.py` (rewrite+classify+weights+half-life); 서비스/그래프 fusion 양쪽 소비, 계약 테스트 `test_retrieval_policy.py`, `test_retrieval_fusion_gate.py` |
| 0.3 Executor transcript window | ✅ | `agent.py` `compact_transcript`/`_truncate_strings`/`TranscriptBudget` (최근 8 전체+이전 요약, 문자열 캡, corrections 3개); critic은 전 스텝+캡; `test_agent_loop_hardening.py` |
| 0.4 Plan←manifest rewrite | ✅ | `normalize_plan` `manifest_steps`/`manifest_rewrite` (순수 파일 계획+누락 시만 개입); `test_agent_loop_hardening.py` |

## Wave 1 — 사용자 체감

| 항목 | 상태 | 구현 |
| --- | --- | --- |
| 1.1 Step 타임라인 스트리밍 | ✅ | `AgentDeps.on_step`+`ctx.on_step` → `agent_live_stream` `event: agent_step` SSE; FE `AgentStepTimeline` 라이브+post-hoc |
| 1.2 출처 카드 → 청크 | ✅ | AnswerProof 출처 클릭 → 저장 본문 모달 (graph node API) |
| 1.3 Watch/job 건강 시그널 | ✅ | `folder_watch` last_errors 노출 + FE WatchHealthCard(마지막 스캔·오류 샘플·"실시간 아님" 문구) |
| 1.4 표면 패리티 매트릭스 | ✅ | `docs/SURFACE_PARITY.md` (✅/◐/—/✖, ✖=백로그) |

## Wave 2 — Graph RAG 품질

| 항목 | 상태 | 구현 |
| --- | --- | --- |
| 2.1 md/code 청킹 | ✅ | `_kg_common.py` `typed_chunks`/`chunk_strategy_for` (plain 바이트 동일 보장 테스트); ingest 4개 관문 배선; `test_typed_chunking.py` 16 |
| 2.2 Embedder fingerprint | ✅ | `retrieval_vector.py` graph_meta 지문, `index_status().embedder`, freshness `stale_embedder`, hybrid `vector_degraded`; `test_vector_freshness.py` |
| 2.3 인용 프롬프트 + grounding bench | ✅ | `models/router.py` `CITATION_INSTRUCTION`+`_compose_system`(4경로 통일); `test_grounding_bench.py` 합성 벤치 게이트 |
| 2.4 PDF 페이지 메타 | ✅ | `pdf_page_offsets`/`page_for_offset` — 구조 오프셋 기반 1-based `page`, 불일치 시 정직한 생략 |
| 2.5 주기적 noise curate 제안 | ✅ | `command_center` hygiene 섹션+quick action (≥200노드·7일 경과), `last_noise_curate_at` graph_meta |

## Wave 3 — 하네스 확장

| 항목 | 상태 | 구현 |
| --- | --- | --- |
| 3.1 Workflow/multi-agent 5시나리오 | ✅ | `tests/unit/test_workflow_scenarios.py` (happy/실패 노드/재개/권한 거부/일관성) |
| 3.2 Playwright first-value E2E | ◐ | 릴리스 증거 캡처(`release:evidence`)가 mock-server 기반 first-value 경로를 커버; 사이드카 연동 nightly E2E는 후속 (아래 "남은 갭") |
| 3.3 Funnel soft gate | ✅ | `scripts/funnel_soft_gate.py` (advisory 기본, `--strict`), `approval_pauses/resumes`+`approval_resume_rate`; `test_funnel_soft_gate.py` |
| 3.4 실모델 agent weekly smoke | ✅ | `scripts/bench_agent_smoke.py` (fail-open, repairs 계측) + `.github/workflows/agent-smoke.yml` (리포트 아티팩트, 절대 게이트 아님); `test_bench_agent_smoke.py` |

## Wave 4 — 범위 확장

| 항목 | 상태 | 구현 |
| --- | --- | --- |
| 매니페스트 Python/Vite React | ✅ | `file_generation.py` `_react_manifest`/`_python_package_manifest`+module-entry HTML 규칙 |
| 쿼리 rewrite / rerank | ◐ | rewrite ✅ (규칙 기반, kill-switch); cross-encoder rerank는 의도적 미착수(리뷰도 "나중") |
| 승인 TTL UX | ✅ | 카운트다운+만료 경고+410 replan 원클릭 (FE) + 백엔드 replan 힌트 |
| Enterprise 승격 검토 모드 | ✅ | `LATTICEAI_GRAPH_PROMOTION_REVIEW`/`GRAPH_PROMOTION_REVIEW` capability, `pending_promotions`+apply/reject API; `test_graph_promotion_review.py` |

## 리뷰 §4.2 루프 이슈 (L1–L8)

- L1 승인 이중화: durable store가 신규 경로를 기본화, 레거시 human_in_loop 호환 유지 (전면 통합은 후속)
- L2 컨텍스트 폭증: ✅ Wave 0.3
- L3 중간 관측: ✅ Wave 1.1
- L4 artifact↔critic: ◐ 번들 검증은 direct+plan rewrite로 커버, critic 프롬프트 연동은 후속
- L5 실행 중 RAG 갱신: 미착수 (후속 백로그)
- L6 Memory 루프: ✅ 터미널 상태별 학습 정책 (`memory_update`)
- L7 Rollback 범위: 미착수 (git 외 스냅샷 정렬은 후속)
- L8 매니페스트 vs 계획: ✅ Wave 0.4

## 남은 갭 (→ 9.9.5에서 마감)

> **해소:** [V9.9.5_WORK_STATUS.md](V9.9.5_WORK_STATUS.md) 참고. 아래 목록은 9.9.4 출하 시점의 역사적 백로그다.

1. 사이드카 연동 Playwright nightly E2E (3.2 잔여)
2. Cross-encoder rerank 옵션
3. 실행 중 워크스페이스 스냅샷 갱신 (L5)
4. rollback: none|git|snapshot 정렬 (L7)
5. critic ↔ artifacts[] 메타 체크리스트 (L4 잔여)
6. VS Code/Telegram 승인 흐름 (SURFACE_PARITY ✖ 갭)
7. 승인 경로 레거시 human_in_loop 정식 폐기 (L1 잔여)

## 검증 스냅샷 (출하 시점)

- 전체 유닛 스위트 green (1595+; 신규 테스트 파일 8종)
- agent_eval 23/23 · retrieval fusion gate · grounding bench · golden filegen
- 실모델 스모크: 로컬 gemma 3태스크 완주 (fail-open 계약 테스트 별도)
- lint(i18n 패리티·bundle budget·OpenAPI drift·debt) + docs 게이트 통과 후 태그
