# V9.9.5 Work Status — 9.9.4 잔여 갭 마감

> **작성일:** 2026-07-26 · **결과:** 9.9.5 — Closed Gaps
> 대상: [V9.9.4_WORK_STATUS.md](V9.9.4_WORK_STATUS.md) "남은 갭" 7항목 + retrieval 분해 리팩터.

## 갭 → 구현

| # | 항목 | 상태 | 구현 |
| --- | --- | --- | --- |
| 1 | 사이드카 연동 Playwright nightly E2E (3.2) | ✅ | `tests/e2e/sidecar-first-value.spec.js`, `scripts/run_sidecar_e2e.mjs`, `playwright.e2e.config.js`, `.github/workflows/e2e-sidecar.yml`, `npm run test:e2e:sidecar` |
| 2 | Cross-encoder rerank 옵션 | ✅ | `lattice_brain/graph/rerank.py` (env `LATTICEAI_CROSS_ENCODER_RERANK`), hybrid_search `rerank` 메타, quality.RerankerInterface 위임; 기본 identity |
| 3 | 실행 중 워크스페이스 스냅샷 갱신 (L5) | ✅ | `files_written` + executor prompt "Files written by this run so far" |
| 4 | rollback: none\|git\|snapshot (L7) | ✅ | `AgentDeps.snapshot_file`/`restore_snapshot`, `_rollback_one` mode 태그, ToolDispatchService ports |
| 5 | critic ↔ artifacts[] 메타 체크리스트 (L4) | ✅ | `artifact_checklist` + critic prompt 주입 (sanitize/repair honesty) |
| 6 | VS Code/Telegram 승인 흐름 | ✅ | VS Code `listApprovals`/`approveAgent`/`rejectAgent` + 토큰 캐시; Telegram awaiting_approval + token resume |
| 7 | 레거시 human_in_loop 정식 폐기(통합) (L1) | ✅ | 별도 `_pending` 제거 → durable `_approvals`/`AgentRunStore` + `legacy_context`; wire `waiting_approval`/`context_id` 유지 |

## 리팩터

| 항목 | 상태 | 구현 |
| --- | --- | --- |
| retrieval read 표면 분해 | ✅ | `lattice_brain/graph/retrieval_reads.py` (`KnowledgeGraphReadsMixin`), store MRO 배선, retrieval.py는 search 표면 유지 |

## 검증

- unit: L4/L5/L7, approval L1, rerank, snapshot ports
- agent_eval 23/23
- sidecar E2E workflow (nightly + path-filtered push)
