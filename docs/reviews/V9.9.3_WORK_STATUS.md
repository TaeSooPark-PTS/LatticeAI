# v9.9.3 작업 현황 — 일시 중지 시점 스냅샷 (2026-07-21)

> **기준 백로그:** [REMAINING_WORK_AFTER_9.9.2.md](REMAINING_WORK_AFTER_9.9.2.md) (22개 항목)
> **상태 (최종):** 22개 백로그 항목 전부 v9.9.3 — Closed Loops 로 릴리스 완료.
> 상세는 [RELEASE_NOTES_v9.9.3.md](../releases/RELEASE_NOTES_v9.9.3.md) 참고. 아래는 작업 당시 스냅샷.

---

## 1. 완료 (Wave 1A — 백엔드 filegen/agent) ✅

백로그 **#1, #2, #21, #22** 구현 완료. 유닛 테스트 1369개 전부 통과(+41 신규), ruff/py3.11 호환 확인.

| # | 항목 | 구현 내용 |
| --- | --- | --- |
| 1 | Artifact Loop (멀티파일 manifest) | `infer_project_manifest`(ko/en, html+css+js 번들) → 파일별 generate/validate → 참조 리페어(`repair_bundle_references`) → 번들 검증(`validate_project_bundle`) → `artifacts[]` + `project.zip_url`. zip 다운로드: `GET /tools/download_zip?path=<dir>` (traversal/symlink/50MB 가드) |
| 2 | WAITING_APPROVAL 승인 흐름 | 승인 필요 plan → `status: "awaiting_approval"` + `approval {token(10분 TTL), expires_at, plan_summary}`. `POST /agent/resume` `{run_id, approval_token, approve, edited_plan?}` — 404/403/410 계약, 토큰 단일 사용, deny → cancelled 기록. fail-closed 게이트는 유지 |
| 21 | Phase budgets | `PhaseBudgets` (plan/execute/verify/memory 토큰 캡, env 오버라이드, 기본 1024/4096/512/256) — agent 루프 5개 max_tokens 지점 배선 |
| 22 | 확장자 확대 + py ast 검증 | `.py`→`ast.parse`(실패 시 리페어), `.js/.jsx/.ts/.tsx` 딜리미터 검증, `.vue/.svelte` 블록 태그 검증 |

신규 테스트: `test_project_manifest_loop.py`(17), `test_agent_approval_flow.py`(13), `test_file_generation.py`(+11).

**주의(릴리스 노트 반영 필요):** 승인 필요 agent run이 이제 FAILED 대신 `awaiting_approval`로 끝남. 승인 대기 상태는 in-process(서버 재시작 시 유실, resume→404). manifest 추론은 웹 번들(html+css+js)만 커버.

## 2. 진행 중 (백그라운드 에이전트 3개 — 완료 대기만) 🔄

| 워크스트림 | 백로그 항목 | 범위 |
| --- | --- | --- |
| Wave 1B 지식 파이프라인 | #3(백엔드), #5, #8, #9, #10, #11 | demo corpus 3문서+원클릭 주입 API, retrieval fusion(쿼리 클래스별 가중치)+벤치 임계값 게이트, folder watch(옵트인·폴링), 웹 추출 품질 CTA, graph curator 노이즈 감소 job, 인용 강제(grounding 배지) |
| Wave 1C 프론트엔드 UX | #4, #7, #17, #18, #19, #20 | 파일 카드 인라인 미리보기(sandbox iframe/모달), 폴더 job 리포트 카드, a11y(포커스 트랩/graph 키보드/reduced-motion), 전역 DnD, 409 rebase UX, 성공 펄스 |
| Wave 2D-1 filegen 하네스 | #12, #13, #14 | agent_eval filegen 시나리오(CI 게이트), bench_models 멀티모델 filegen 리포트(fail-open), golden files(`tests/fixtures/filegen/`) |

## 3. 남은 작업 (미착수) ⏸️

### 구현

| 순서 | 항목 | 백로그 | 내용 |
| --- | --- | --- | --- |
| 1 | Wave 2D-2 하네스 나머지 | #6, #15, #16 | automation dry-run("지금 한 번 실행"+실행 로그 표면화+실패→Review 큐) 백엔드, Knowledge Pipeline E2E 결정론 하네스(temp 폴더→ingest→hybrid search→context_quality→suggestions), UX 퍼널 런타임 메트릭(TTFV, file request→real file rate, code-only rate, NEEDS_REVIEW rate) — **1B 완료 후 시작해야 충돌 없음** |
| 2 | Wave 2E 프론트엔드 2차 | #2 UI, #3 UI, #6 UI | awaiting_approval 인라인 승인 카드(승인하고 실행/수정해서 실행), First Value Loop 온보딩 트랙(demo corpus 주입→추천 질문 칩→출처 하이라이트→HTML 생성), automation dry-run 버튼/마지막 실행 결과 표시 — **1A~1C의 실제 API 계약에 맞춰 배선** |

### 통합 검증

- `npm run frontend:openapi` 재생성(신규 엔드포인트: `/agent/resume` 확장, `/tools/download_zip`, demo-corpus, watch, curator job 등) + drift check
- `npm run lint` (i18n ko/en 패리티, bundle budget, legacy debt 게이트 포함)
- `npm run typecheck`, `npm run test:frontend`, 전체 pytest, `npm run build:assets`
- `node scripts/run_python.mjs scripts/agent_eval.py` / `brain_quality_eval.py` (CI에서 도는 게이트)

### 9.9.3 릴리스 (모두 미착수)

1. `python scripts/bump_version.py 9.9.3` (18개 파일 단일 커맨드)
2. **`scripts/check_current_release_docs.mjs`의 `releaseTheme` 하드코딩 교체** ("Artifact Trust" → 9.9.3 테마)
3. 문서: `RELEASE_NOTES_v9.9.3.md`, RELEASE.md `## v9.9.3 — <테마>`, docs/CHANGELOG.md `## [9.9.3]`, RELEASE_NOTES.md `[v9.9.3 - <테마>]` + "8.0.0 through 9.9.3", README "The current release is **9.9.3 — <테마>**", ARCHITECTURE.md 아티팩트 맵(`Lattice AI_9.9.3_aarch64.dmg` 등 정확한 이름 5종), FEATURE_STATUS/PRIVACY/SECURITY 버전 참조, OPERATIONS.md 정합성
4. `npm run release:evidence` — README가 요구하는 `output/release/v9.9.3/` 미디어 생성
5. `npm run release:artifacts` — tgz/wheel/sdist/vsix/**DMG**(tauri build) + `release:validate`(--require-dmg)
6. commit → `git tag v9.9.3` → push(main+tag) → GitHub Release 생성(`gh release create`) → CI(ci.yml + release.yml) 통과 확인

## 4. 재개 방법

"작업 재개해줘"라고 하면: 진행 중이던 3개 워크스트림 결과 반영 → 위 표 순서대로 D-2 → E → 통합 검증 → 릴리스 순으로 진행.
