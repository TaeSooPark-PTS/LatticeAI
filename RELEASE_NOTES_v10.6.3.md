# Lattice AI 10.6.3 — Loud Limits

릴리스일: 2026-08-04

## 한 줄 요약

**한계가 있는 건 문제가 아닙니다. 한계를 말하지 않는 게 문제입니다.** 10.6.3은
시스템이 조용히 대충 하던 자리를 찾아, 하던 일을 그대로 하되 **그렇게 하고 있다고
말하게** 만듭니다.

## 왜 이 릴리스인가

2026-08 코드 리뷰에서 나온 지적은 대부분 "동작하지 않는다"가 아니라 **"동작하는
척한다"**였습니다. 검색은 색인의 5%만 보고 전부를 본 것처럼 답했고, 수집 잡은
재시작하면 흔적 없이 사라졌으며, 쿠키 인증 쓰기는 브라우저 정책 하나에 기대고
있었습니다. 전부 정상 화면에서는 보이지 않는 것들입니다.

## 무엇이 바뀌었나

### 검색이 자기 한계를 말합니다

sqlite-vec가 없으면 벡터 검색은 brute-force로 떨어지는데, 이때 색인 최신순으로
1만 행만 보고 **전체를 본 것처럼** 응답했습니다. 20만 행 브레인에서는 recall이
사실상 "최신 5%"였고 응답 어디에도 그 말이 없었습니다.

이제 응답에 **"M개 중 N개를 봤다"**가 실립니다. 잘린 기준이 유사도가 아니라
색인 최신순이라는 사실, 즉 **오래된 자료는 비교조차 되지 않았다**는 점까지
적습니다. `LATTICEAI_VECTOR_MAX_CANDIDATES=0`으로 전체 스캔을 택할 수 있습니다.

### 수집이 재시작을 견딥니다

백그라운드 수집 큐가 프로세스 메모리에만 있어서, 도중에 재시작하면 진행 상태가
사라졌습니다. 사용자에게는 잡이 그냥 없어진 것으로 보였고 다시 돌리면 끝난 일을
되풀이했습니다. 이제 SQLite에 저장되어 **남은 항목부터 이어갑니다.** 프로세스가
죽어 `running`으로 남은 잡은 로드할 때 `partial`/`queued`로 정직하게 보고합니다 —
실제로 도는 게 없으니까요.

### 쓰기 요청의 출처를 확인합니다

쿠키로 인증된 mutating 요청이 `SameSite=Lax` 하나에만 기대고 있었습니다. Origin과
Referer를 확인하는 가드를 넣었고, 리버스 프록시 뒤 공개 호스트명은
`LATTICEAI_CSRF_TRUSTED_ORIGINS`로 허용합니다.

함께 8개 라우터가 제각각 파생하던 workspace 스코프 해석을 한 모듈로 모았습니다.
헤더·쿼리·바디가 서로 다른 workspace를 가리키면 **조용히 하나를 고르지 않고
403**을 냅니다.

### 애매하면 쓰지 않습니다

파일 쓰기 도구가 판단이 애매한 경우 통과시키던 경로를 fail-closed로 바꿨습니다.
기존 파일이 사라지는 비용이 한 번 거절하는 비용보다 훨씬 큽니다. 모델 스트리밍
실패도 `ModelStreamError`로 구분해 올립니다 — 이전에는 빈 응답과 실패가 호출부에서
구분되지 않았습니다.

### 복원 직후 검색이 됩니다

백업 임포트가 그래프만 채우고 벡터 인덱스는 예전 상태로 남겨, 복원 직후 검색이
방금 임포트한 내용을 찾지 못했습니다. 이제 임포트 직후 재색인합니다.

### 빌드가 소스와 맞는지 확인합니다

Playwright 비주얼 스위트는 커밋된 `static/app/` 산출물을 구동하는데, 그 디렉터리가
현재 소스에서 빌드됐다는 걸 아무도 보장하지 않았습니다. 컴포넌트를 고치고 빌드를
잊으면 CI가 **이미 존재하지 않는 UI를 상대로** 초록불을 냈습니다. 신선도 게이트를
추가했습니다.

### 대화 화면

SSE 스트리밍 파서를 분리해 테스트 가능하게 만들고, 대화 중 자동 하단 고정 동작을
고쳤습니다.

## 검증

- Python 2138 통과 / 11 스킵 (10.6.2 대비 +29)
- 프론트엔드 420 통과 (42 파일)
- lint · typecheck · 버전 일관성 12파일 · 문서 동기화 전부 통과

## 업그레이드 노트

동작이 바뀌는 곳이 두 군데 있습니다.

- **검색 응답에 recall 리포트 필드가 추가됩니다.** 기존 필드는 그대로입니다.
- **workspace 선택자가 불일치하면 이제 403입니다.** 이전에는 조용히 하나가
  선택됐습니다. 헤더·쿼리·바디에 서로 다른 workspace를 보내던 클라이언트가 있다면
  일치시켜야 합니다.
