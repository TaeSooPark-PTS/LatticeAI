# v10.10.0 — Quiet Station (2026-08-06)

Brain 대화 홈을 "고요한 스테이션"으로 다시 지은 릴리스입니다. 첫 화면은
입력창 하나에 집중하고, 나머지는 전부 **원할 때 피어나는 어포던스**가
됐습니다. 그리고 프론트엔드 테스트 커버리지를 100%로 올려 CI가 그 바닥을
지키게 했습니다.

## 홈 재설계 — Quiet Station

- **통계는 배지로.** "기억 13개, 주제 299개를 기억하고 있어요" 문장이 인사말
  줄을 차지하고 있었습니다. 이제 `기억 13 · 주제 299` 칩 하나가 인사말 옆에
  앉고, 마우스를 올리거나 클릭하면 요약 그래프(기억/주제/연결 막대)와 기억
  지도 바로가기가 담긴 팝오버가 열립니다. 호버로 열린 메뉴를 곧바로 이은
  클릭이 도로 닫아버리지 않도록 350ms 디바운스를 두었습니다.
- **여섯 개 캡처 칩이 + 하나로.** 문서 · 이미지 · 파일 · 폴더 · 노트 · 웹이
  컴포저의 `+ 추가` 뒤로 접혔습니다. 호버가 열고, 클릭이 고정하고, Escape와
  바깥 클릭이 닫습니다. 업로드가 진행 중이거나 폴더/노트/웹 입력 폼이 열려
  있으면 포인터가 떠나도 닫히지 않습니다 — 입력 중에 메뉴가 사라지는 일은
  없어야 하니까요. 대화 화면의 컴포저도 같은 + 메뉴를 씁니다.
- **모델 배너가 상태 필로.** 화면 상단을 가득 채우던 "아직 Brain의
  목소리(모델)가 준비되지 않았습니다" 배너가 히어로 오른쪽의
  `모델 준비 필요` 필 + CTA 하나가 됐습니다. 전체 문장은 툴팁과 접근성
  이름(aria-label)으로 그대로 전달됩니다.
- **두 개의 서랍이 독(dock)으로.** 지난 대화 · Brain이 정리한 내용 · 기억
  지도가 화면 하단에 쌓이는 대신, 조용한 레일(대화 · 통계 · 기억 지도)이
  데스크톱에서는 캔버스 왼쪽 기둥으로, 좁은 화면에서는 폴드 위 가로 줄로
  섭니다. 레일을 누르면 포커스 트랩이 걸린 서랍이 화면 위로 열립니다 —
  본문에 카드를 쌓지 않습니다. 서랍 안 기억 링의 peek이 열려 있으면 첫
  Escape는 peek을, 두 번째가 서랍을 닫습니다.
- **테두리는 그림자로, 색은 두 곳으로.** 카드 보더를 걷어내고 부드러운
  그림자와 은은한 글라스로 층을 만들었습니다. 따뜻한 포인트 컬러는 정확히 두
  곳에만 남습니다: 보내기 버튼과 모델 준비 CTA.

### 재설계가 데려온 수정들

- 히어로의 `overflow: clip`이 통계 팝오버를 통째로 삼키고 있었습니다 —
  클립은 원래 유기체 헤일로를 가두는 용도였고, 헤일로는 10.6.x부터
  `--aura-blur`로 배너 크기에 맞게 조여져 있어 더는 가둘 것이 없습니다.
- 포털로 띄운 서랍 안에서 `.brain-care-panel` 계열(그 자체가
  `display: grid` + `overflow: hidden` + backdrop-filter)의 그리드 행이
  2px로 붕괴해 버튼이 클릭 불가능해졌습니다. 서랍 본문을 그리드 대신 flex
  컬럼으로 바꿔 패널이 콘텐츠 높이를 되찾게 했습니다.
- 새 + 버튼의 aria-label에 "메뉴 열기"가 들어가 셸의 메뉴 버튼과 role 매칭이
  충돌했습니다. 라벨을 "자료 추가: …"로 바꿨습니다.

## 프론트엔드 커버리지 100%

- `frontend/src` 전체가 statements / branches / functions / lines 모두
  100%입니다(`all: true` — 테스트가 임포트하지 않은 파일도 분모에 들어가는
  정직한 설정 그대로).
- `vitest.config.ts`에 thresholds 100이 걸려 있어 한 줄만 빠져도 로컬과 CI가
  같이 실패합니다. CI의 프론트엔드 테스트 스텝은 커버리지 게이트로 돌립니다.
- 0%였던 파일들(BrainCarePanel, MemoryRings, ProductFlow, CytoscapeGraph,
  BrainGraphLayer 등)과 8%대의 api/client.ts까지 전부 실사용 경로 기준으로
  테스트를 붙였습니다. 커버리지를 위한 `/* v8 ignore */` 주석은 사용하지
  않습니다.

## 릴리스 히스토리 정리

- 공개 릴리스 히스토리가 **9.0.0부터** 시작합니다. 8.x 릴리스 노트 9개
  파일이 트리를 떠났고 README · RELEASE_NOTES.md · RELEASE.md ·
  docs/CHANGELOG.md가 같은 경계를 말합니다.
  `test_markdown_current_release_references_match_release`가 새 경계를
  고정합니다.

## 게이트 정합화

- `product_readiness`의 first-run 증거가 새 구조(BrainHomeDock)를
  가리킵니다.
- 고아가 된 i18n 키(`brain.hero.summary`, `brain.home.insights`)를 키 삭제로
  정리했습니다 — 고아 베이스라인은 자라지 않습니다.
- `scripts/release_screen_claims.json`에 10.10.0의 04-brain-chat-home 변경
  주장을 등록했습니다(min 3%).

## 빌드 산출물

`dist/ltcai-10.10.0-py3-none-any.whl`, `dist/ltcai-10.10.0.tar.gz`,
`ltcai-10.10.0.tgz`, `dist/ltcai-10.10.0.vsix`,
`src-tauri/target/release/bundle/dmg/Lattice AI_10.10.0_aarch64.dmg`.
와일드카드 업로드는 사용하지 않습니다.
