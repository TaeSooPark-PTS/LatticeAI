# Lattice AI 10.3.0 — Measured Ground

릴리스일: 2026-07-29

## 한 줄 요약

**이 프로젝트가 스스로에 대해 보고하던 숫자 세 개가 틀렸습니다.** 그걸 바로잡은
것이 이번 릴리스에서 가장 값진 부분입니다.

## 1. 프론트엔드 커버리지는 측정된 적이 없었습니다

vitest는 기본적으로 **테스트가 이미 import한 파일만** 리포트합니다. 테스트가
아예 없는 모듈은 분모에서 조용히 사라집니다.

```
before (all 미설정):   54%   ← 테스트된 파일들만의 커버리지
after  (all: true):  28.5%   ← 실제
```

같은 코드, 같은 테스트. 달라진 건 **분모가 정직해진 것**뿐입니다.

프론트 테스트를 154 → **208개**로 늘렸습니다. 페이지 단위 테스트가 하나도
없던 곳에 처음 생겼습니다 — 설정·기억·라이브러리·자료·작업 전부.

`frontend/src/test/renderPage.tsx` 하니스가 `latticeApi` 전체를 스텁해서,
브라우저 없이도 **오류 상태·빈 상태·양쪽 언어**에 도달할 수 있습니다. Playwright는
행복 경로만 지나가므로 이런 상태들은 그동안 검증된 적이 없었습니다.

## 2. Python 커버리지 80%는 71.6%였습니다

```toml
omit = ["*/tests/*"]   # repo-relative 'tests/...' 에 매치되지 않음
```

그래서 **테스트 스위트가 자기 자신을 세고 있었습니다.** 테스트 파일은 정의상
처음부터 끝까지 실행되니 9포인트가 부풀려졌습니다.

```
omit = ["tests/*", "scripts/*"]   →   71.6%   (하한 70% CI 강제)
```

Python 테스트는 1,786 → **1,896개**. 그동안 테스트가 없던 경계들에 붙였습니다:

| 경계 | 왜 중요한가 | 테스트 |
| --- | --- | --- |
| Telegram 허용목록 | **인터넷에서 닿는 유일한 표면.** 설정이 없거나 비었거나 깨졌을 때 전부 거부해야 함 | 26 |
| `run_command` 봉쇄 | 허용목록·셸 연산자·절대경로·심볼릭 탈출·환경 격리·타임아웃 | 34 |
| 감사 로그 / 민감도 | 부분 기록이 정상 기록을 덮으면 안 됨, 마스킹은 알아볼 수는 있어야 함 | 22 |
| 모델 로드 동의 | 설치·다운로드는 옵트인, 스모크 실패는 "준비됨"이 아님 | 14 |

## 3. mypy: 13개 → 193 / 270개 모듈

그리고 그 과정에서 **진짜 결함 3개**를 찾았습니다.

```python
# lattice_brain/runtime/hooks.py — 오류 처리기 '안'에서
except (OSError, json.JSONDecodeError, ...) as exc:
    LOGGER.warning("...at %s: %s", self._path, exc)   # self._path 는 없음
```

레지스트리를 못 읽는 **복구 가능한** 상황이 `AttributeError`로 바뀌어 있었습니다.
오류 처리기 안의 오류가 가장 나쁜 종류입니다.

나머지 둘: `Iterable`을 import 없이 애노테이션에 사용(`from __future__ import
annotations` 덕분에 런타임에는 잠복), `None`일 수 있는 값에 `.get` 호출.

## 4. 채팅 기록 함수가 조립 루트를 떠났습니다

`app_factory._build` 안 66줄. **리댁션 이후 메시지가 어떤 모습인지, 감사 로그에
무엇이 남는지를 결정하는 함수**인데 애플리케이션 전체를 띄우지 않고는 실행할 수
없었습니다.

순서가 곧 계약이라, 테스트 14개가 그 순서를 못 박습니다:

```
1. 리댁션 먼저   — 감사·저장·그래프 전부 리댁션된 텍스트만 본다
2. 감사 후 저장  — 저장이 실패해도 그 메시지가 있었다는 기록은 남는다
3. 파이프라인 경유 — store 직접 쓰기는 provenance와 훅을 우회한다
4. 그래프 실패는 메시지를 잃지 않는다
```

**추출하다 만난 함정:** 중첩 `def`는 늦게 바인딩되는 이름을 **호출 시점에**
해석하지만, 빌더 호출은 즉시 해석합니다. 의존성 둘이 `_build` 아래쪽에서
바인딩되므로 순진한 추출은 import 시점에 `UnboundLocalError`를 냅니다.
클로저는 클로저로 남기고 위임하게 했으며, 주석에 이유를 적었습니다.

## 5. 측정되지 않는 것은 적어두었습니다

`docs/MYPY_BACKLOG.md` — 아직 타입체크 밖인 **77개 모듈**을 오류 수와 함께
작은 것부터 나열했습니다. 경계가 "언젠가 하겠다"가 아니라 **사실**이 됩니다.

ARCHITECTURE.md에 **Verification Surface** 다이어그램을 추가해, 어떤 수치가 CI를
막고 어떤 수치는 보고만 되는지 구분했습니다. 프론트 커버리지는 점선 — 오늘
수치로 하한을 걸면 거기 얼어붙습니다.

## 정직한 자기 평가

리뷰 기준으로 다시 매기면 **약 88 / 100**입니다. 95는 아닙니다.

| 영역 | 10.1.1 | 10.2.0 | 10.3.0 | 95를 위해 남은 것 |
| --- | ---: | ---: | ---: | --- |
| 아키텍처·모듈성 | 15 | 16 | **16** | `_build` 1,300줄 실제 분해 |
| 테스트·검증 | 12 | 16 | **17** | 프론트 28.5→70%, Python 71.6→85% |
| 보안·프라이버시 | 11 | 18 | **19** | 서버 i18n, 남은 S-룰 면제 실제 수정 |
| 코드 품질·툴링 | 13 | 17 | **18** | mypy 77개 잔여, UP 룰그룹 |
| 릴리스·문서·정직성 | 20 | 20 | **20** | — |
| | **71** | **87** | **88** | |

**남은 7점의 정직한 비용:** 프론트 커버리지를 70%로 올리려면 약 2,500 statement,
Python을 85%로 올리려면 약 4,400 statement를 덮어야 합니다. 대략 테스트 파일
30~40개 분량입니다. `_build` 분해는 런타임 컨텍스트 객체 도입이 필요한 아키텍처
변경이고, 지역 바인딩 ~34개를 옮겨야 합니다.

이번 릴리스에서 하지 못했고, **했다고 말하지 않겠습니다.**

## 검증

| 게이트 | 결과 |
| --- | --- |
| pytest | 1,896 passed / 11 skipped |
| Python 커버리지 | **71.6%** (하한 70%, CI 강제) |
| 프론트 단위 | **208** passed |
| 프론트 커버리지 | **28.5%** (보고, 미강제) |
| mypy | **193 / 270** 모듈 clean |
| ruff | clean (10개 룰그룹) |
| 시각 테스트 | 20/20 |
| agent_eval | 23/23 |
| 번들 | 102.1 KiB / 150 KiB |

## 업그레이드

할 일이 없습니다. 동작 변경 없음, API 변경 없음.

## 알려진 한계

- `app_factory._build` ~1,300줄 유지 (history writer만 추출)
- 프론트 커버리지 28.5% — 보고되지만 게이트 아님
- mypy 밖 77개 모듈 — `docs/MYPY_BACKLOG.md`
- 서버가 만드는 일부 안내 문장은 여전히 영어
